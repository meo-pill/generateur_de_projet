/**
	* \file main.c
	* \brief Le fichier source de l'executable final.
	* \author ~~AUTEURS~~
	* \version 1.0
	* \date ~~DATE~~
	*
	* Le fichier source du programme final du projet ~~PROJET~~.
	*
	*/

// INCLUSION(S) DE(S) BIBLIOTHEQUE(S) NÉCÉSSAIRE(S)
#include "./include/commun.h"

// CRÉATION(S) DE(S) MACRO(S)

// CRÉATION(S) DE(S) CONSTANTE(S) NUMÉRIQUE(S)

// CRÉATION(S) D(ES) ÉNUMÉRATION(S)

// CRÉATION(S) D(ES) STRUCTURE(S) ET D(ES) UNIONS(S)

// CRÉATION(S) DE(S) CONSTANTE(S) DE STRUCTURE(S)

// CRÉATION(S) DE(S) FONCTION(S)

// PROGRAMME PRINCIPALE
/** \brief Le programme principale du projet
	* \author ~~AUTEURS~~
	* \param argc Le nombre d'argumant passé dans '*argv[]'.
	* \param *argv[] La liste des arguments envoyé au programme.
	* \return 0 si tout c'est bien passé, le code de l'erreur dans tous les autres cas.
	*
	*/
int main( int argc , char *argv[] ){
	// INITIALISATION DE(S) VARIABLE(S)
		/* Création des variables d'états */
	Err_t *err=NULL;
	Err_cause_e codeErr = E_C_OK;
		/* Création des autres variables */

	// INSTRUCTION(S)

	// FIN DU PROGRAMME
	codeErr = E_C_OK;
Quit:	/* Destruction des objets */
	FIN_PROG(err,codeErr)
}
// PROGRAMME PRINCIPALE


// #####-#####-#####-#####-##### FIN PROGRAMMATION #####-#####-#####-#####-##### //
